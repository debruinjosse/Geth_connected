# GETH brand mark

**Canonical file:** `geth-crest-mark.png` — use everywhere (header, dashboard, cards, hero mockup, favicon).

**Code:** `lib/brand.ts`, `components/BrandLogo.tsx`, `components/BrandMarkIcon`.

Recognition photos and other imagery are separate assets — do not replace those with the crest.
